   // const userID = req.body.userid

  // const user = await User.findById(userID).populate('role');
  
      // const sidebarMenuIds = user.role.sidebarMenus;
  
      // // Find sidebar menus based on the sidebarMenuIds
      // const sidebarMenus = await SidebarMenu.find({ _id: { $in: sidebarMenuIds } });
  
      // // Map the sidebarMenus to only include menu names
      // const menuNames = sidebarMenus.map(menu => menu.menu);
